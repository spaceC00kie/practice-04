//given an array print out the itm of the array through an arrow function and use forEach

const printCows = (cow) =>
  cows.forEach((cow) => {
    console.log(cows);
  });

const cows = ["brown", "black", "spotted"];

console.log(cows);
